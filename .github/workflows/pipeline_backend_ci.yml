name: Build, Test and Try to Publish Backend
on:
  push:
    paths:
      - 'BackEnd/**'
    branches:
      - 'feature/**'
  pull_request:
    branches:
      - develop
    
jobs:
  build-test-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up ASP.NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'

      - name: Restore dependencies
        run: dotnet restore BackEnd/GitHubSimulator/GitHubSimulator.sln

      - name: Build solution
        run: dotnet build BackEnd/GitHubSimulator/GitHubSimulator.sln --configuration Release --no-restore

      - name: Run tests
        run: dotnet test BackEnd --no-restore --no-build --configuration Release

      - name: Publish modular monolith service
        run: dotnet publish BackEnd/GitHubSimulator/ --configuration Release --no-build